circuit:
Custom(DP, 1, prefix=custom_dp::, params=DP_Params, vars=DP_Vars, updater=update_vars, compute=root_compute);
    Parallel(P1);
        C(C1);
        RVs(Vin, ?);

inputs:
Vin

outputs:
V:C1

meta:
include:"custom_diode_pair.h"
